Config = {}

Config.PassWord = "1302" -- Password when the menu is opened

Config.UseAKey = true -- (if false open with Config.Command)

Config.Key = 121 -- The key used for open the menu (only if Config.UseAKey is set to true) default = 121 (insert)

Config.Command = "menu"

Config.EnableMouse = false -- when open the menu navigate with the cursor ?

-- If you want to translate go to AdminByNess/cl.lua